#Local MongoDB config
spring:
  data:
    mongodb:
      authentication-database: admin
      username: root
      password: root
      database: user_db
      port: 27017
      host: localhost
  application:
    name: BootMongo
server:
  port: 8102
  context-path: /user